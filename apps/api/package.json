{
  "name": "@yakudoku/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --no-sync uvicorn yakudoku_api.main:app --reload --port 8000",
    "lint": "uv run --no-sync ruff check .",
    "typecheck": "uv run --no-sync mypy .",
    "test": "uv run --no-sync pytest"
  }
}
