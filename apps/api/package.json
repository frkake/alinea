{
  "name": "@yakudoku/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "db:migrate": "uv run --no-sync alembic upgrade head",
    "lint": "uv run --no-sync ruff check .",
    "typecheck": "uv run --no-sync mypy .",
    "test": "uv run --no-sync pytest"
  }
}
